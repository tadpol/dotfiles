# Aliases for limactl that are similar to the docker/nerctl aliases
#
alias lc=limactl
alias lcup='limactl start'
alias lcstop='limactl stop'
alias lcs='limactl shell'
alias lce='limactl edit'

autoload -Uz lcl

